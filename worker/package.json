{
  "name": "cf-video-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute cf-video-metadata --local --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute cf-video-metadata --remote --file=src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260421.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.12.0"
  }
}